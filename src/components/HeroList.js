import React from "react";
import HeroListItem from "./HeroListItem";

const HeroList = () => {
  return (
    <div>
      <HeroListItem />
      <HeroListItem />
      <HeroListItem />
      <HeroListItem />
    </div>
  );
};

export default HeroList;
